/* BNS Push Config V1
   Plaats in HOOFDMAP als:
   /firebase-push-config.js

   Firebase Console:
   Projectinstellingen > Cloud Messaging > Web Push certificates > Generate key pair
*/
window.BNS_PUSH_VAPID_KEY = "VUL_HIER_VAPID_KEY_IN";
